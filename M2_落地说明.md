# M2 最终落地说明（基于 M1 资料回收 + M2 直落地）

本仓库在原 **M1 GitHub 部署包**基础上完成：

- 回收 **M1 工程期内未完成/部分完成项**（CI/类型检查/OpenAPI/Contract tests 等）
- 直接落地 **M2 关键闭环**（文件中心 Signed URL + 文档元数据 + 扫描 worker 骨架 + 审计）

> 说明：由于这里无法直接操作你的云资源（Cloud Run / Cloud SQL / GCS），与“staging 真实跑通”相关的部分以 **可直接执行的 gcloud 脚本 + runbook**形式落地；你只需要替换项目/区域/实例名即可执行。

---

## 1. M1 未完成项回收（已落地）

- ✅ **Typecheck 全量恢复**
  - 根脚本恢复为：`pnpm -r --if-present typecheck`
  - 不再过滤 `@aicasa/api` / `@aicasa/api-client`

- ✅ **Contract tests 变为必跑**
  - 新增根脚本：`pnpm contract:test`
  - CI 中新增 step：docker-compose 启 DB + flyway migrate + 起 API（dev:once）再跑 contract tests
  - contract tests 扩充到覆盖 **文件中心闭环**（presign->upload->complete->download）

- ✅ **OpenAPI lint 恢复严格度**
  - `.spectral.yaml` 启用并阻断：`operation-operationId`、`operation-description`、`oas3-api-servers`
  - 新增 `scripts/openapi_autofix.py`：一键补齐缺失 metadata，并同步 YAML/JSON（contracts + docs/key_docs）

- ✅ **SCA/Trivy 阻断策略**
  - CI 中 `trivy fs` 对 `CRITICAL,HIGH` 直接阻断（可在 `.trivyignore` 后续加白名单）

---

## 2. M2 关键闭环（已落地）

### 2.1 文件中心：Presign Upload -> Upload -> Complete -> Download

已新增/补齐 API（位于 `apps/api/src/routes/files.ts` & `apps/api/src/routes/documents.ts`）：

- `POST /v1/files/presign-upload`
- `PUT  /v1/files/_local-upload/:uploadId`（仅 local provider，用于本地/CI 演示）
- `POST /v1/files/complete-upload`
- `GET  /v1/documents`
- `GET  /v1/documents/:docId`
- `PATCH /v1/documents/:docId`（If-Match 并发控制）
- `GET  /v1/documents/:docId/download`
  - `Accept: application/json` 返回 envelope + `downloadUrl`
  - 否则 302 redirect 到 `downloadUrl`

支持两种 provider：

- `STORAGE_PROVIDER=local`：用于 CI/本地跑通闭环（上传/下载走 API 内部 endpoint）
- `STORAGE_PROVIDER=gcs`：用于 staging/prod（upload/download 使用真正的 GCS signed URL）

### 2.2 扫描工作流骨架（Cloud Run Job 形态）

- 新增 `apps/scan-worker`：最小扫描 worker
  - 读取 `file_scan_jobs` 中 `PENDING` 任务
  - 目前“骨架行为”：将对应 `documents.scan_status` 从 `SCANNING` 更新为 `READY`（后续可替换为 ClamAV / 第三方 AV / DLP 流程）

### 2.3 审计日志（Audit Log）

- 新增写入工具：`apps/api/src/audit.ts`
- 文件相关关键动作写入 `audit_log`（presign / upload / complete / download / patch）

### 2.4 RLS 改进：missing_ok + SERVICE 角色

为支持后台任务与匿名上下文不“直接报错”，增加：

- `V5__rls_missing_ok.sql`：把 `current_setting('app.org_id')` 统一改为 `current_setting('app.org_id', true)`
- `V4__rls_service_role.sql`：增加 `app.role='SERVICE'` 的 bypass policy（用于 scan-worker 等后台任务）

---

## 3. 本地运行（推荐）

### 3.1 启 DB + migrate + seed

```bash
cd .

docker compose up -d db

docker compose --profile tools run --rm flyway

# 可选：灌一份示例数据
docker compose exec -T db psql -U aicasa -d aicasa_dev -f /seed/dev_seed.sql
```

### 3.2 启 API

```bash
export APP_ENV=dev
export DATABASE_URL=postgres://aicasa:aicasa@localhost:5432/aicasa_dev
export SESSION_SECRET=dev
export CSRF_SECRET=dev
export COOKIE_SECURE=false
export COOKIE_SAMESITE=lax
export STORAGE_PROVIDER=local
export FILE_SCAN_ENABLED=false

pnpm -C apps/api dev:once
```

### 3.3 一键跑 contract tests（推荐）

```bash
pnpm contract:test
```

---

## 4. Staging 落地（脚本 + Runbook）

与 Cloud Run / Cloud SQL / GCS staging 相关的可执行脚本统一放在：

- `infra/staging/`

请按 runbook 替换：`PROJECT_ID / REGION / INSTANCE_NAME / BUCKET_NAME / IMAGE_TAG`。

---

## 5. 关键文件索引

- CI：`.github/workflows/ci.yml`
- OpenAPI：`contracts/openapi/openapi_v1.0.6_enveloped.yaml`
- OpenAPI 修复脚本：`scripts/openapi_autofix.py`
- Contract tests：`packages/contract-tests/src/contract.test.ts`
- 文件中心 API：`apps/api/src/routes/files.ts` / `apps/api/src/routes/documents.ts`
- 扫描 worker：`apps/scan-worker/src/worker.ts`
- DB migrations：`infra/db/flyway/V2__*.sql` ~ `V5__*.sql`
