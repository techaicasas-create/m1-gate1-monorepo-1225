/**
 * 注意：generated 目录由 scripts/openapi_codegen_ts_fetch.sh 生成。
 * 真实项目中建议将生成物封装在一层，避免业务方直接依赖生成路径。
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore
export * from "../generated/ts-fetch-client/index";
