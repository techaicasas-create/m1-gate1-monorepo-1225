{
  "name": "@aicasa/scan-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "dev:once": "tsx src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/worker.js",
    "lint": "eslint '**/*.{ts,tsx,js,jsx}' --no-error-on-unmatched-pattern",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo '(TODO) add worker tests'"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "eslint": "^8.57.0",
    "@types/node": "^20.11.0",
    "@types/pg": "^8.11.6"
  }
}
